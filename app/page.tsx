export default function Home() {
  return (
    <main className="page-shell">
      <section className="hero">
        <p className="eyebrow">Differ test fixture</p>
        <h1>Small checkout app for import and adaptation testing</h1>
        <p className="lede">
          This app gives Differ a real checkout surface to import, observe, assess,
          and adapt without any database, auth, or custom server behavior.
        </p>
        <a className="primary-link" href="/checkout">
          Open checkout
        </a>
      </section>
    </main>
  );
}
